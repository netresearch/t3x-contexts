name: Lock Threads

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  lock:
    uses: netresearch/.github/.github/workflows/lock.yml@f87ee244a6e0d78c24e0bd58794563ab8d916943 # main
    permissions:
      issues: write
      pull-requests: write
