name: Lock Threads

on:
  schedule:
    - cron: '0 1 * * *'
  workflow_dispatch:

permissions: {}

jobs:
  lock:
    uses: netresearch/.github/.github/workflows/lock.yml@main
    permissions:
      issues: write
      pull-requests: write
